# Numerology Engine v1 evidence record

Candidate branch: `fed-numerology-canonical`
Target: `release/ios-4.0.0-federation-integrated`

This candidate is calculation-layer only. It must not change interpretive prose until its deterministic calculator tests and the Federation subsystem gate pass.

Required evidence before merge:
- Node 22 dependency install
- workspace build
- TypeScript check
- date-only numerology regression
- Moon/Rising golden regression
- privacy/local-first regression

A failed or missing measurement remains failed or unavailable; it is never converted to PASS.
