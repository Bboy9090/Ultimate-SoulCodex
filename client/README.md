## Legacy `client/` app (non-canonical)

This folder is a **legacy** Vite SPA implementation. The canonical frontend is:

- `apps/web`

This `client/` tree is kept temporarily for reference and safe migration work during Phase 2, but it should **not** be used for new feature work.

