// Legacy Compatibility entrypoint retained only for import compatibility.
// The active Foundation relationship model is the canonical Hub → Explorer /
// Compare journey. The former profile-vs-profile overall-score experience is
// intentionally retired rather than left as a second product contract.
export { default } from "./CompatibilityHubPage";
