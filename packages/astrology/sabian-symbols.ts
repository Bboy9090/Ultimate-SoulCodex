// Sabian Symbols remain outside the governed Foundation identity contract.
// Missing degree symbols must never be invented by an AI model and presented as
// authentic traditional symbols.
export async function calculateSabianSymbols(
  sunLongitude: number,
  moonLongitude: number,
  ascendantLongitude: number,
): Promise<never> {
  void sunLongitude;
  void moonLongitude;
  void ascendantLongitude;
  throw new Error('sabian_symbols_unavailable:governed_360_degree_corpus_required');
}
