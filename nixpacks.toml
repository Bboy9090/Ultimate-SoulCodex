# Nixpacks config for Railway
# Builds the Next.js src/ app (Emergent psychology engine)
# next.config has output: "standalone" so the server is self-contained

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build:next"]

[start]
cmd = "node .next/standalone/server.js"
