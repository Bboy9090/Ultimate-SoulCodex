# Nixpacks config for Railway
# Builds both the Next.js upgraded frontend (src/) and the Express API (server/)
# The Express server auto-detects and serves the Next.js static export.

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build:next", "npm run build"]

[start]
cmd = "npm start"
